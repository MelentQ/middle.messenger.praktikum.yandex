<div class="form">
    <fieldset class="form__inputs">
        {{#each inputs}}
            {{{this}}}
        {{/each}}
    </fieldset>

    <div class="form__actions">
        {{#each actions}}
            {{{this}}}
        {{/each}}
    </div>
</div>